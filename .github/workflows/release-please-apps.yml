on:
  push:
    branches:
      - staging

name: release-please

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
        - name: Run Release Please Action
        uses: googleapis/release-please-action@v4
